server:
  port: 8093
spring:
  profiles:
    active: dev
  application:
    name: junhua-flowable
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8849
        namespace: public
        username: nacos
        password: nacos
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true